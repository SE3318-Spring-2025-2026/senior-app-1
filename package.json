{
  "name": "senior-app-1",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm --prefix backend run dev & npm --prefix frontend run dev; wait",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev"
  },
  "dependencies": {
    "react-markdown": "^10.1.0",
    "rehype-sanitize": "^6.0.0"
  }
}
